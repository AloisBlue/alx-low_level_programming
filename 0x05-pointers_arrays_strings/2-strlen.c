#include "main.h"
#include <string.h>

/**
 * _strlen - calculate length of a string
 *
 * @s: character variable
 *
 * Return: Return length of string
*/
int _strlen(char *s)
{
	return (strlen(s));
}
